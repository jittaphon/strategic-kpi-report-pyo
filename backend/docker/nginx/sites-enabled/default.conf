# ไฟล์: STRATEGIC-KPI-REPORT-PYO/docker/nginx/sites-enabled/default.conf
# Nginx Site Configuration สำหรับ Lumen Backend
# ไฟล์นี้จะบอก Nginx ว่าจะ serve Lumen application อย่างไร

server {
    listen 80;
    server_name localhost;
    root /var/www/html/public; # <<<--- สำคัญมาก: ชี้ไปที่ public folder ของ Lumen

    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-XSS-Protection "1; mode=block";
    add_header X-Content-Type-Options "nosniff";

    index index.html index.htm index.php;

    charset utf-8;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass 127.0.0.1:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}